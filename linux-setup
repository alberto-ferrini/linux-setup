#!/bin/bash

set -eu

version=1.0.0
echo 🔵 Linux Setup $version

echo ⏳ Checking for update...
# Download the content of the file into a variable
online_content=$(curl -s https://raw.githubusercontent.com/alberto-ferrini/linux-setup/refs/heads/main/linux-setup)

# Use grep to find the line starting with 'linuxSetupVersion=' and sed to extract the value after '='
online_version=$(echo "$online_content" | grep '^version=' | sed 's/^version=//')

if [ "$version" = "$online_version" ]; then
    echo "🟢 Linux Setup is up to date"
else
    echo "🟡 Update is available (v. $online_version)"
fi